<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/jpg" href="/images/logo_El_Manzano.jpg">
  <link rel="stylesheet" href="/stylesheets/styles.css">
  <title><%= title %> - Portal de Pagos</title>
  <style>
    /* ============================================
       BLOQUE: .result
       Resultado de operación de pago
       ============================================ */
    .result {
      max-width: 600px;
      margin: 2rem auto;
      padding: 2rem;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    /* Modificador: Resultado pendiente */
    .result--pending {
      border-top: 5px solid #ffc107;
    }

    .result__icon {
      font-size: 4rem;
      margin-bottom: 1rem;
    }

    .result__title {
      font-size: 1.8rem;
      color: #856404;
      margin-bottom: 0.5rem;
    }

    .result__subtitle {
      color: #666;
      margin-bottom: 2rem;
    }

    /* Elemento: Información del pago */
    .result__info {
      background: #f8f9fa;
      padding: 1.5rem;
      border-radius: 8px;
      margin: 1.5rem 0;
      text-align: left;
    }

    .result__info-row {
      margin: 0.5rem 0;
      display: flex;
      justify-content: space-between;
    }

    .result__info-label {
      color: #333;
      font-weight: bold;
    }

    .result__info-value {
      color: #666;
      font-family: monospace;
    }

    .result__info-value--pending {
      color: #856404;
      font-weight: bold;
    }

    /* Elemento: Instrucciones */
    .result__instructions {
      background: #fff3cd;
      padding: 1.5rem;
      border-radius: 8px;
      margin: 1.5rem 0;
      text-align: left;
      border-left: 4px solid #ffc107;
    }

    .result__instructions-title {
      color: #856404;
      margin-bottom: 1rem;
    }

    .result__instructions-text {
      color: #856404;
      margin: 0.5rem 0;
    }

    .result__instructions-list {
      margin: 1rem 0;
      padding-left: 1.5rem;
      color: #856404;
    }

    .result__instructions-item {
      margin: 0.5rem 0;
    }

    /* Elemento: Nota importante */
    .result__note {
      background: #e8f4fd;
      padding: 1rem;
      border-radius: 8px;
      margin: 1rem 0;
      color: #0c5460;
      font-size: 0.9rem;
    }

    .result__note-label {
      font-weight: bold;
    }

    .result__note-small {
      display: block;
      margin-top: 0.5rem;
    }

    /* Elemento: Acciones */
    .result__actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: 2rem;
      flex-wrap: wrap;
    }

    .result__btn--back {
      background: #19a8b6;
      color: white;
      text-decoration: none;
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      transition: background 0.3s;
    }

    .result__btn--back:hover {
      background: #148a96;
    }
  </style>
</head>

<body>
  <!-- Header -->
  <header class="header">
    <div class="header__logo">
      <img src="<%= municipalidad.logos.principal %>" alt="Logo <%= municipalidad.nombre %>" class="header__logo-img">
    </div>
    <div class="header__title">
      <h2>Portal de Pago<br><%= municipalidad.nombreCompleto %></h2>
    </div>
    <div class="header__icon">
      <img src="<%= municipalidad.logos.secundario %>" alt="Logo Alcald+IA" class="header__icon-img">
    </div>
  </header>

  <!-- Contenido Principal -->
  <main class="main">
    <section class="result result--pending">
      <div class="result__icon">⏳</div>
      <h1 class="result__title">Pago Pendiente</h1>
      <p class="result__subtitle">Tu pago está siendo procesado</p>

      <div class="result__info">
        <p class="result__info-row">
          <span class="result__info-label">Número de operación:</span>
          <span class="result__info-value"><%= external_reference %></span>
        </p>
        <p class="result__info-row">
          <span class="result__info-label">Estado:</span>
          <span class="result__info-value result__info-value--pending">Pendiente de acreditación</span>
        </p>
      </div>

      <div class="result__instructions">
        <h3 class="result__instructions-title">ℹ️ Información importante</h3>
        <p class="result__instructions-text">Tu pago fue registrado pero está pendiente de confirmación.</p>
        <p class="result__instructions-text">Esto puede ocurrir cuando:</p>
        <ul class="result__instructions-list">
          <li class="result__instructions-item">Elegiste pagar en efectivo (RapiPago, Pago Fácil, etc.)</li>
          <li class="result__instructions-item">El pago está siendo verificado por el banco</li>
          <li class="result__instructions-item">Usaste transferencia bancaria</li>
        </ul>
      </div>

      <div class="result__note">
        <span class="result__note-label">📋 Guardá tu número de operación:</span> <%= external_reference %>
        <small class="result__note-small">Una vez acreditado el pago, podrás ver el comprobante en este portal.</small>
      </div>

      <div class="result__actions">
        <a href="/" class="result__btn--back">🏠 Volver al inicio</a>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p class="footer__text">&copy; Power by Alcald+IA. Todos los derechos reservados.</p>
  </footer>
</body>

</html>