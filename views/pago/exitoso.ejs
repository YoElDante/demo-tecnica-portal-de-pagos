<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/jpg" href="/images/logo_El_Manzano.jpg">
  <link rel="stylesheet" href="/stylesheets/styles.css">
  <title><%= title %> - Portal de Pagos</title>
  <style>
    /* ============================================
       BLOQUE: .result
       Resultado de operación de pago
       ============================================ */
    .result {
      max-width: 600px;
      margin: 2rem auto;
      padding: 2rem;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    /* Modificador: Resultado exitoso */
    .result--success {
      border-top: 5px solid #28a745;
    }

    .result__icon {
      font-size: 4rem;
      margin-bottom: 1rem;
    }

    .result__title {
      font-size: 1.8rem;
      color: #28a745;
      margin-bottom: 0.5rem;
    }

    .result__subtitle {
      color: #666;
      margin-bottom: 2rem;
    }

    /* Elemento: Información del pago */
    .result__info {
      background: #f8f9fa;
      padding: 1.5rem;
      border-radius: 8px;
      margin: 1.5rem 0;
      text-align: left;
    }

    .result__info-row {
      margin: 0.5rem 0;
      display: flex;
      justify-content: space-between;
    }

    .result__info-label {
      color: #333;
      font-weight: bold;
    }

    .result__info-value {
      color: #666;
      font-family: monospace;
    }

    .result__info-value--success {
      color: #28a745;
      font-weight: bold;
    }

    /* Elemento: Sección de email */
    .result__email {
      background: #e8f5e9;
      padding: 1.5rem;
      border-radius: 8px;
      margin: 1.5rem 0;
      text-align: left;
    }

    .result__email-title {
      color: #2e7d32;
      margin-bottom: 1rem;
    }

    .result__email-form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .result__email-input {
      padding: 0.8rem;
      border: 1px solid #c8e6c9;
      border-radius: 6px;
      font-size: 1rem;
    }

    .result__checkbox {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .result__checkbox input[type="checkbox"] {
      width: 18px;
      height: 18px;
    }

    /* Elemento: Botón de email */
    .result__btn--email {
      background: #28a745;
      color: white;
      border: none;
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 1rem;
      transition: background 0.3s;
    }

    .result__btn--email:hover {
      background: #218838;
    }

    /* Elemento: Acciones */
    .result__actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: 2rem;
      flex-wrap: wrap;
    }

    .result__btn--back {
      background: #19a8b6;
      color: white;
      text-decoration: none;
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      transition: background 0.3s;
    }

    .result__btn--back:hover {
      background: #148a96;
    }
  </style>
</head>

<body>
  <!-- Header -->
  <header class="header">
    <div class="header__logo">
      <img src="<%= municipalidad.logos.principal %>" alt="Logo <%= municipalidad.nombre %>" class="header__logo-img">
    </div>
    <div class="header__title">
      <h2>Portal de Pago<br><%= municipalidad.nombreCompleto %></h2>
    </div>
    <div class="header__icon">
      <img src="<%= municipalidad.logos.secundario %>" alt="Logo Alcald+IA" class="header__icon-img">
    </div>
  </header>

  <!-- Contenido Principal -->
  <main class="main">
    <section class="result result--success">
      <div class="result__icon">✅</div>
      <h1 class="result__title">¡Pago Exitoso!</h1>
      <p class="result__subtitle">Tu pago ha sido procesado correctamente</p>

      <div class="result__info">
        <p class="result__info-row">
          <span class="result__info-label">Número de operación:</span>
          <span class="result__info-value"><%= external_reference %></span>
        </p>
        <p class="result__info-row">
          <span class="result__info-label">ID de pago:</span>
          <span class="result__info-value"><%= payment_id %></span>
        </p>
        <p class="result__info-row">
          <span class="result__info-label">Estado:</span>
          <span class="result__info-value result__info-value--success">Aprobado</span>
        </p>
      </div>

      <div class="result__email">
        <h3 class="result__email-title">📧 Recibir comprobante por email</h3>
        <form class="result__email-form" action="/pago/guardar-email" method="POST">
          <input type="hidden" name="external_reference" value="<%= external_reference %>">

          <input type="email" name="email" class="result__email-input" placeholder="tu@email.com" value="<%= typeof email_actual !== 'undefined' ? email_actual : '' %>" required>

          <div class="result__checkbox">
            <input type="checkbox" id="guardar_email" name="guardar_email" checked>
            <label for="guardar_email">Guardar este email para futuras operaciones</label>
          </div>

          <button type="submit" class="result__btn--email">📤 Enviar comprobante</button>
        </form>
      </div>

      <div class="result__actions">
        <a href="/" class="result__btn--back">🏠 Volver al inicio</a>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer class="footer">
    <p class="footer__text">&copy; Power by Alcald+IA. Todos los derechos reservados.</p>
  </footer>
</body>

</html>